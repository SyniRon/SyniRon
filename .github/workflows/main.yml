name: Profile charts
on:
  schedule: [{ cron: "45 3 * * *" }]
  workflow_dispatch:
  push: { branches: ["main"] }

jobs:
  contrib-3d:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: yoshi389111/github-profile-contrib-3d@v0.9.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: ${{ github.repository_owner }}
      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile-contrib-3d/*.svg
          git diff --staged --quiet || git commit -m "chore: update 3d contrib chart"
          git push

  summary-cards:
    runs-on: ubuntu-latest
    needs: contrib-3d
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: vn7n24fzkq/github-profile-summary-cards@v0.7.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}
